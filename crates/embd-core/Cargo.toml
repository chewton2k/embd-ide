[package]
name = "embd-core"
version = "0.1.0"
edition = "2021"
description = "Shared types, error handling, and event primitives for embd IDE"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
