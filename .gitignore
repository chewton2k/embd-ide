# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
docs/superpowers
target

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.claude/*
.claude
CLAUDE.md
.vscode
todo.md
docs/
scripts/bump-version.sh
# `tests/` was previously gitignored, which silently kept the entire
# unit-test suite out of CI. Re-enable tracking. If you want to keep
# scratch test files locally, place them outside `tests/unit/` and
# `tests/setup.ts` / `tests/mocks/`, or add a more specific ignore.
!tests/
!tests/**
scripts/test.sh
