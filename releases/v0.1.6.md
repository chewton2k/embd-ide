- ctrl/cmd + F has some problems, glitches out and won't let me close it, im able to make multiple finders, breaking the feature
- add export config file for settings so you can import them to the ide and it would automatically change the settings
- include a selection in settings to set how many recent projects they want to save, make the min 0 and the max 20 
- make it in the settings so the user can set how many max tabs they want
- fixed closing window bug where permissions didn't allow it to close the application
- when i click on a file in the changes to see the changes, i should be able to click on it again to close those changes for that specific file
- if im opening a folder that is in the recent projects, just open the saved session instead of starting a new one.
- there is a bug where you cannot zoom in an out on pdfs
- cute commands like /tableflip and /tableunflip in my ide kind of like discord



- complete rust rewrite 